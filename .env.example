# Copy this file to `.env` (or export these as environment variables) and fill in
# your own values. Do NOT commit real credentials.

DB_USERNAME=arham
DB_PASSWORD=arham123
DB_URL=jdbc:postgresql://localhost:5432/library_db